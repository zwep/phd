from data_postproc.objective.reconstruction.check_convergence import VisualizeConvergence
import matplotlib.pyplot as plt
import re


"""
Here we can check how stuff looks like..

We messed around with it a bit.

Now it has improved
"""

dtest = '/home/bugger/Documents/data/model_run/direct/model_metrics'
vis_obj = VisualizeConvergence(dtest)
